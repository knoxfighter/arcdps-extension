@PACKAGE_INIT@

include ( "${CMAKE_CURRENT_LIST_DIR}/ArcdpsExtensionTargets.cmake" )

set_target_properties(ArcdpsExtension::ArcdpsExtension PROPERTIES
  INTERFACE_INCLUDE_DIRECTORIES "${_IMPORT_PREFIX}/include"
)